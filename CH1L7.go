// CH 1 L7 the computation process 

//Compilation errors happen when the code is compiled. They're the good kind: a program with a compiler error won't build, so it can't ship to production.


//Runtime errors happen while the program is running. They're worse because they can crash a program that's already in use.

package main



import "fmt"

func main() {
	fmt.Println("The compiled textio server is starting") // In the lesson there were errors in the boot.dev that I have to debug 
}
